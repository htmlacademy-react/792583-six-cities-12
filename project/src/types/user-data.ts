export type UserData = {
  id: number;
  email: string;
  token: string;
};

export type UserAuth = {
  id: number;
  isPro: boolean;
  name: string;
  avatarUrl: string;
  email: string;
  token: string;
}
